[pytest]
# rootdir se fija en nodeboard-backend/ (donde vive este ini), así que
# pythonpath = . agrega este directorio a sys.path y `app` se resuelve
# sin importar desde qué carpeta se invoque pytest.
pythonpath = .
testpaths = tests
