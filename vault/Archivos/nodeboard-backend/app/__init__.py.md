**Ruta:** `nodeboard-backend/app/__init__.py`

## Responsabilidad
Marca `app/` como paquete Python. Archivo vacío — permite los imports `from app.main import ...`, `from app import models, schemas`.

## Exporta
- (nada) — archivo vacío

## Importa
- (nada)

## Importado por
- Implícito: habilita todos los imports del paquete `app` (main, models, schemas, database, tests)
