**Ruta:** `src/vite-env.d.ts`

## Responsabilidad
Declaración de tipos de Vite para el proyecto. Habilita el tipado de `import.meta.env` (usado en `api.ts` con `VITE_API_URL`) y de imports de assets.

## Contenido
- `/// <reference types="vite/client" />`

## Importado por
- (implícito) — el compilador TS lo incluye globalmente
